<van-tabbar active="{{ active }}" bind:change="onChange" active-color="#fff" inactive-color="gainsboro">
  <van-tabbar-item wx:for="{{ list }}" wx:key="index" >
    <image
      slot="icon"
      src="{{ item.normal }}"
      mode="aspectFit"
      style="width: 70px; height: 38px;"
    />
    <image
      slot="icon-active"
      src="{{ item.active }}"
      mode="aspectFit"
      style="width: 70px; height: 38px;"
    /> 
    {{item.text}}
  </van-tabbar-item>
</van-tabbar>